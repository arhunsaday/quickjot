PORT=3001
DATABASE_PATH=data/quickjot.sqlite
# Exact public origin used to reject cross-origin writes and socket connections.
APP_ORIGIN=http://localhost:5173
MAX_ROOMS=10000
DISABLE_CREATION=false
DISABLE_AI=false
# No host AI key needed: users supply their own key for each browser session.
